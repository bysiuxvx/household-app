version: 1.0
runtime: nodejs22
working_directory: apps/backend  # relative to repo root

build:
  commands:
    build:
      - pnpm install
      - pnpm --filter backend build
    run:
      command: pnpm start

port: 8080
