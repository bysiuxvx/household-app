// Shared type definitions
export * from './types';

// Example type
export interface User {
  id: string;
  name: string;
  email: string;
}

export interface Household {
  id: string;
  name: string;
  members: string[];
}
